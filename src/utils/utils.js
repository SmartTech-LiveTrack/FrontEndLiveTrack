// export const float = (e) => {
//     document.querySelector([name=${e.target.name}]).value ?
//       document.querySelector([name=${e.target.name}]).classList.add('is-filled') :
//       document.querySelector([name=${e.target.name}]).classList.remove('is-filled')
// }